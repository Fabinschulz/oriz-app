import 'package:flutter/material.dart';

class AppColors {
  // Brand & Accent
  static const Color primary = Color(0xFFC70B0B); // Oriz Red
  static const Color accent = Color(0xFF4A90E2);

  // Semantic (Mais suaves para não agredir a visão)
  static const Color income = Color(0xFF34A853);
  static const Color expense = Color(0xFFE53935);

  // Categorias (Paleta Harmônica)
  static const Color food = Color(0xFFFF9800); // Mercado
  static const Color transport = Color(0xFF1E88E5); // Gasolina
  static const Color education = Color(0xFF8E24AA); // Escola
  static const Color utility = Color(0xFFFDD835); // Luz
  static const Color leisure = Color(0xFFF06292); // Shopping/Natação
  static const Color health = Color(0xFF26A69A); // Academia
  static const Color tech = Color(0xFF00ACC1); // Internet

  // Neutrals
  static const Color background = Color(0xFFFDFDFD);
  static const Color surface = Colors.white;
  static const Color textPrimary = Color(0xFF212121);
  static const Color textSecondary = Color(0xFF616161);
  static const Color border = Color(0xFFEEEEEE);
}
