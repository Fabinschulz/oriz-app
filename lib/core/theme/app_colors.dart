import 'package:flutter/material.dart';

class AppColors {
  // Brand & Accent
  static const Color primary = Color(0xFFC70B0B); // Oriz Red
  static const Color accent = Color(0xFF4A90E2);

  // Semantic
  static const Color income = Color(0xFF2E7D32); // Verde Esmeralda fechado
  static const Color expense = Color(0xFFD32F2F);
  static const Color balance = Color(0xFF1A237E);

  // --- Paleta de Categorias Atualizada ---
  static const Color food = Color(0xFFFF9800); // Mercado (Laranja vibrante)
  static const Color restaurant = Color(
    0xFFE64A19,
  ); // Restaurante (Tom de terracota/fome)
  static const Color coffee = Color(0xFF795548); // Café (Marrom grão)

  static const Color transport = Color(0xFF1976D2); // Gasolina (Azul Petróleo)
  static const Color publicTransport = Color(
    0xFF0288D1,
  ); // Transp. Público (Azul Céu)

  static const Color education = Color(0xFF673AB7); // Escola (Roxo Intelectual)
  static const Color utility = Color(
    0xFFFBC02D,
  ); // Luz (Amarelo Âmbar - menos "neon")

  static const Color water = Color(0xFF03A9F4); // Água (Azul Claro)
  static const Color phone = Color(0xFF4DB6AC); // Telefone (Azul Vivo)
  static const Color creditCard = Color(
    0xFF8E24AA,
  ); // Cartão de Crédito (Roxo Vivo)

  static const Color rent = Color(
    0xFF455A64,
  ); // Aluguel (Azul Acinzentado - estabilidade)

  static const Color leisure = Color(
    0xFFEC407A,
  ); // Shopping/Natação (Rosa Moderno)
  static const Color park = Color(0xFF689F38); // Parque (Verde Folha)
  static const Color travel = Color(0xFF0097A7); // Viagem (Azul Turquesa)

  static const Color health = Color(0xFF009688); // Academia (Teal Saúde)
  static const Color pharmacy = Color(
    0xFFEF5350,
  ); // Farmácia (Vermelho Clínico Suave)

  static const Color tech = Color(0xFF3F51B5); // Internet/Tech (Índigo)
  static const Color gift = Color(0xFFFF5252); // Presente (Coral)
  static const Color pet = Color(0xFF8D6E63); // PetShop (Bege/Marrom Quente)

  // Neutrals
  static const Color background = Color(0xFFF8F9FA);
  static const Color surface = Colors.white;
  static const Color textPrimary = Color(0xFF1A1C1E);
  static const Color textSecondary = Color(0xFF44474E);
  static const Color border = Color(0xFFDDE2EA);
}
